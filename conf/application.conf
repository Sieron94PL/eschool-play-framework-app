# This is the main configuration file for the application.
# https://www.playframework.com/documentation/latest/ConfigFile


#Models
ebean.default = ["models.*"]

# Default database configuration using MySQL database engine
# Connect to playdb as playdbuser

db.default.driver = com.mysql.jdbc.Driver
db.default.url = "jdbc:mysql://localhost:3306/play_eschool"
db.default.username = root
db.default.password = "root"

play.filters.headers.contentSecurityPolicy = null

recaptcha.privateKey = 6LeCYDsUAAAAAIdzBkRnUjwgmNerPgNi1LZl6cr1
recaptcha.publicKey = 6LeCYDsUAAAAAO317hS6uI9vzGh9kseqcRxq69wD

#Deadbolt module
play {
  modules {
    enabled += be.objectify.deadbolt.java.DeadboltModule
    enabled += modules.CustomDeadboltHook
  }
}
play.filters.enabled += "play.filters.csrf.CSRFFilter"
play.i18n.langs = [ "en", "pl" ]

play.mailer {
  host = smtp.gmail.com
  port = 465 // (defaults to 25)
  ssl = yes // (defaults to no)
  tls = no // (defaults to no)
  tlsRequired = no // (defaults to no)
  user = "eschool.pl@gmail.com" // (optional)
  password = "politechnika69" // (optional)
  debug = no // (defaults to no, to take effect you also need to set the log level to "DEBUG" for the application logger)
  timeout = null // (defaults to 60s in milliseconds)
  connectiontimeout = null // (defaults to 60s in milliseconds)
  mock = no // (defaults to no, will only log all the email properties instead of sending an email)
}